<dialog class="dialog">
    <header class="dialog__header">
        <h2 class="dialog__headline">{{ headline }}</h2>
        <button class="button button--icon button--primary">
            {% include "icons/close.svg"%}}
        </button>
    </header>
    {{ content | safe }}
</dialog>
