<div class="form__item">
    <label class="form__label form__label--inline">
        <input 
            type="radio" 
            name="{{ id }}" 
            value="{{ value }}" 
            required>
            {{ label }}
    </label>
</div>