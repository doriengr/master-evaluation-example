<div class="form__item">
    <label class="form__label" for="{{ id }}">{{ label }}</label>
    <input
        type="{{ type }}"
        id="{{ id }}"
        name="{{ id }}"
        required
        placeholder="{{ instructions }}"
        class="form__input"
    >
</div>